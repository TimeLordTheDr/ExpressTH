extends layout

block content
  h2 Welcome, #{name}!
  form(action='/hello', method='post')
    label(for='username') Please enter you name:
    input(type='text', name='username')
    button(type='submit') Submit